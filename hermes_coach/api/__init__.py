"""Coach-only loopback API.

Requirement families: `HC-PRODUCT`, `HC-PRIVACY`; sources `SRC-104…106`.

Separate from the Hermes dashboard app: no TUI dispatch, no PTY, no admin
routes, no account model. Binds loopback only.
"""
